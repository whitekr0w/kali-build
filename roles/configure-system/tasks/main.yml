---
# tasks file for configure-system
- name: "[+] Install Bash config"
  ansible.builtin.include_tasks:
    file: bash.yml

- name: "[+] Install Tmux config"
  ansible.builtin.include_tasks:
    file: tmux.yml

- name: "[+] Install QTerm config"
  ansible.builtin.include_tasks:
    file: qterm.yml

- name: "[+] Install XFCE config"
  ansible.builtin.include_tasks:
    file: xfce.yml